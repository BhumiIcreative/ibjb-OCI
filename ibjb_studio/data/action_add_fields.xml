<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- sale_subscription -->
        <record id="model_sale_order_subscription_add_fields" model="ir.actions.server">
            <field name="name">Migrate studio Sale Subscription fields</field>
            <field name="model_id" ref="sale.model_sale_order"/>
            <field name="binding_model_id" ref="sale.model_sale_order"/>
            <field name="binding_view_types">form,list</field>
            <field name="state">code</field>
            <field name="code">if records: records.Update_sales_studio_fields()</field>
        </record>

        <!-- res_partner -->
        <record id="model_res_partner_add_fields" model="ir.actions.server">
            <field name="name">Migrate studio partner fields</field>
            <field name="model_id" ref="sale.model_res_partner"/>
            <field name="binding_model_id" ref="sale.model_res_partner"/>
            <field name="binding_view_types">form,list</field>
            <field name="state">code</field>
            <field name="code">if records: records.Update_partner_studio_fields()</field>
        </record>

        <!-- purchase_order -->
        <record id="model_purchase_order_add_fields" model="ir.actions.server">
            <field name="name">Migrate studio purchase order fields</field>
            <field name="model_id" ref="purchase.model_purchase_order"/>
            <field name="binding_model_id" ref="purchase.model_purchase_order"/>
            <field name="binding_view_types">form,list</field>
            <field name="state">code</field>
            <field name="code">if records: records.Update_purchase_order_studio_fields()</field>
        </record>

        <!-- stock quant -->
        <record id="model_stock_quant_add_fields" model="ir.actions.server">
            <field name="name">Migrate studio stock quant fields</field>
            <field name="model_id" ref="stock.model_stock_quant"/>
            <field name="binding_model_id" ref="stock.model_stock_quant"/>
            <field name="binding_view_types">form,list</field>
            <field name="state">code</field>
            <field name="code">if records: records.Update_stock_quant_studio_fields()</field>
        </record>

        <!-- Maintenance request -->
        <record id="model_stock_quant_add_fields" model="ir.actions.server">
            <field name="name">Migrate studio Maintenance request fields</field>
            <field name="model_id" ref="maintenance.model_maintenance_request"/>
            <field name="binding_model_id" ref="maintenance.model_maintenance_request"/>
            <field name="binding_view_types">form,list</field>
            <field name="state">code</field>
            <field name="code">if records: records.Update_maintenance_request_studio_fields()</field>
        </record>

        <!-- Account Move -->
        <record id="model_account_move_add_fields" model="ir.actions.server">
            <field name="name">Migrate studio Account Move fields</field>
            <field name="model_id" ref="account.model_account_move"/>
            <field name="binding_model_id" ref="account.model_account_move"/>
            <field name="binding_view_types">form,list</field>
            <field name="state">code</field>
            <field name="code">if records: records.Update_account_move_studio_fields()</field>
        </record>

        <!-- Maintenance Equipment -->
        <record id="model_stock_quant_add_fields" model="ir.actions.server">
            <field name="name">Migrate studio Maintenance Equipment fields</field>
            <field name="model_id" ref="maintenance.model_maintenance_equipment"/>
            <field name="binding_model_id" ref="maintenance.model_maintenance_equipment"/>
            <field name="binding_view_types">form,list</field>
            <field name="state">code</field>
            <field name="code">if records: records.Update_maintenance_equipment_studio_fields()</field>
        </record>

        <!-- Family -->
        <record id="model_family_add_fields" model="ir.actions.server">
            <field name="name">Migrate studio Maintenance Equipment fields</field>
            <field name="model_id" ref="ibjb_studio.model_family"/>
            <field name="binding_model_id" ref="ibjb_studio.model_family"/>
            <field name="binding_view_types">form,list</field>
            <field name="state">code</field>
            <field name="code">if records: records.Update_family_studio_fields()</field>
        </record>

        <!-- Product Price list -->
        <record id="model_product_pricelist_add_fields" model="ir.actions.server">
            <field name="name">Migrate studio Product Pricelist fields</field>
            <field name="model_id" ref="product.model_product_pricelist"/>
            <field name="binding_model_id" ref="product.model_product_pricelist"/>
            <field name="binding_view_types">form,list</field>
            <field name="state">code</field>
            <field name="code">if records: records.Update_product_pricelist_studio_fields()</field>
        </record>

        <!-- Account Partner Category -->
        <record id="model_account_partner_cat_add_fields" model="ir.actions.server">
            <field name="name">Migrate studio Account Partner Category fields</field>
            <field name="model_id" ref="ibjb_studio.model_account_partner_cat"/>
            <field name="binding_model_id" ref="ibjb_studio.model_account_partner_cat"/>
            <field name="binding_view_types">form,list</field>
            <field name="state">code</field>
            <field name="code">if records: records.Update_account_partner_cat_studio_fields()</field>
        </record>

        <!-- Lot Motif Retrait -->
        <record id="model_lot_motif_retrait_add_fields" model="ir.actions.server">
            <field name="name">Migrate studio Stock Lot Withdrawal Reasons fields</field>
            <field name="model_id" ref="ibjb_studio.model_lot_motif_retrait"/>
            <field name="binding_model_id" ref="ibjb_studio.model_lot_motif_retrait"/>
            <field name="binding_view_types">form,list</field>
            <field name="state">code</field>
            <field name="code">if records: records.Update_lot_motif_retrait_studio_fields()</field>
        </record>

        <!-- Maintenance Etapes Pret -->
        <record id="model_maintenance_etapes_pret_add_fields" model="ir.actions.server">
            <field name="name">Migrate studio Maintenance Loan Steps fields</field>
            <field name="model_id" ref="ibjb_studio.model_maintenance_etapes_pret"/>
            <field name="binding_model_id" ref="ibjb_studio.model_maintenance_etapes_pret"/>
            <field name="binding_view_types">form,list</field>
            <field name="state">code</field>
            <field name="code">if records: records.Update_maintenance_etapes_pret_studio_fields()</field>
        </record>

        <!-- Maintenance Version -->
        <record id="model_maintenance_version_add_fields" model="ir.actions.server">
            <field name="name">Migrate studio Maintenance Version fields</field>
            <field name="model_id" ref="ibjb_studio.model_maintenance_version"/>
            <field name="binding_model_id" ref="ibjb_studio.model_maintenance_version"/>
            <field name="binding_view_types">form,list</field>
            <field name="state">code</field>
            <field name="code">if records: records.Update_maintenance_version_studio_fields()</field>
        </record>

        <!-- Naf Code -->
        <record id="model_naf_code_add_fields" model="ir.actions.server">
            <field name="name">Migrate studio Naf Code fields</field>
            <field name="model_id" ref="ibjb_studio.model_naf_code"/>
            <field name="binding_model_id" ref="ibjb_studio.model_naf_code"/>
            <field name="binding_view_types">form,list</field>
            <field name="state">code</field>
            <field name="code">if records: records.Update_naf_code_studio_fields()</field>
        </record>
    </data>
</odoo>
