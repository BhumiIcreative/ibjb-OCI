<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- sale_subscription -->
        <record id="model_sale_order_subscription_add_fields" model="ir.actions.server">
            <field name="name">Migrate studio Sale Subscription fields</field>
            <field name="model_id" ref="sale.model_sale_order"/>
            <field name="binding_model_id" ref="sale.model_sale_order"/>
            <field name="binding_view_types">form,list</field>
            <field name="state">code</field>
            <field name="code">if records: records.Update_sales_studio_fields()</field>
        </record>

        <!-- res_partner -->
        <record id="model_res_partner_add_fields" model="ir.actions.server">
            <field name="name">Migrate studio partner fields</field>
            <field name="model_id" ref="sale.model_res_partner"/>
            <field name="binding_model_id" ref="sale.model_res_partner"/>
            <field name="binding_view_types">form,list</field>
            <field name="state">code</field>
            <field name="code">if records: records.Update_partner_studio_fields()</field>
        </record>

        <!-- purchase_order -->
        <record id="model_purchase_order_add_fields" model="ir.actions.server">
            <field name="name">Migrate studio purchase order fields</field>
            <field name="model_id" ref="purchase.model_purchase_order"/>
            <field name="binding_model_id" ref="purchase.model_purchase_order"/>
            <field name="binding_view_types">form,list</field>
            <field name="state">code</field>
            <field name="code">if records: records.Update_purchase_order_studio_fields()</field>
        </record>

         <!-- stock quant -->
        <record id="model_stock_quant_add_fields" model="ir.actions.server">
            <field name="name">Migrate studio stock quant fields</field>
            <field name="model_id" ref="stock.model_stock_quant"/>
            <field name="binding_model_id" ref="stock.model_stock_quant"/>
            <field name="binding_view_types">form,list</field>
            <field name="state">code</field>
            <field name="code">if records: records.Update_stock_quant_studio_fields()</field>
        </record>
    </data>
</odoo>
